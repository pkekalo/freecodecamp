// Instructions: Print both properties of the dog object to your console.

// (This is a final code)
// (All FreeCodeCamp tests are passed)// (Init)

let dog = {
  name: "Spot",
  numLegs: 4
};

console.log(dog.name);
console.log(dog.numLegs);